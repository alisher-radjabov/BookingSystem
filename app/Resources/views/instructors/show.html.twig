{% extends 'base.html.twig' %}

{% block body %}
    <h1>Instructor</h1>

    <table>
        <tbody>
            <tr>
                <th>Instrname</th>
                <td>{{ instructor.instrName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('instructors_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('instructors_edit', { 'id': instructor.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
